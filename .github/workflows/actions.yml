name: actions
on: [push]
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "run setup"
  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - run: echo "run build"
